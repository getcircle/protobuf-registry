package services.user.actions.verify_verification_code;

// GENERATED OPTIONS FOR: JAVA
option java_package = "com.rhlabs.protobufs.services.user.actions.verify_verification_code";

message RequestV1 {
    optional uint32 version = 1 [default = 1];
    optional string user_id = 2;
    optional string code = 3;
}

message ResponseV1 {
    optional uint32 version = 1 [default = 1];
    // TODO this should just return an error if verification failed
    optional bool verified = 2;
}
