package services.user.actions.bulk_create_users;

import "protobufs/services/user/containers.proto";

message RequestV1 {
    optional uint32 version = 1 [default = 1];
    repeated services.user.containers.UserV1 users = 2;
}

message ResponseV1 {
    optional uint32 version = 1 [default = 1];
    repeated services.user.containers.UserV1 users = 2;
}
