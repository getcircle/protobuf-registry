package services.note.actions.get_notes;

import "protobufs/services/note/containers/note.proto";

message RequestV1 {
    optional uint32 version = 1 [default = 1];
    optional string owner_profile_id = 2;
    optional string for_profile_id = 3;
}

message ResponseV1 {
    optional uint32 version = 1 [default = 1];
    repeated services.note.containers.note.NoteV1 notes = 2;
}
