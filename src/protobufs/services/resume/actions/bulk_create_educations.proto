package services.resume.actions.bulk_create_educations;

import "protobufs/services/resume/containers.proto";

message RequestV1 {
    optional uint32 version = 1 [default = 1];
    repeated services.resume.containers.EducationV1 educations = 2;
}

message ResponseV1 {
    optional uint32 version = 1 [default = 1];
    repeated services.resume.containers.EducationV1 educations = 2;
}
