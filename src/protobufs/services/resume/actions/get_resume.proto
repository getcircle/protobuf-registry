package services.resume.actions.get_resume;

import "protobufs/services/resume/containers/resume.proto";

message RequestV1 {
    optional uint32 version = 1 [default = 1];
    optional string user_id = 2;
}

message ResponseV1 {
    optional uint32 version = 1 [default = 1];
    optional services.resume.containers.resume.ResumeV1 resume = 2;
}
