package services.resume.actions.create_company;

import "protobufs/services/resume/containers/resume.proto";

message RequestV1 {
    optional uint32 version = 1 [default = 1];
    optional services.resume.containers.resume.CompanyV1 company = 2;
}

message ResponseV1 {
    optional uint32 version = 1 [default = 1];
    optional services.resume.containers.resume.CompanyV1 company = 2;
}
