package services.resume.actions.bulk_create_positions;

import "protobufs/services/resume/containers.proto";

message RequestV1 {
    optional uint32 version = 1 [default = 1];
    repeated services.resume.containers.PositionV1 positions = 2;
}

message ResponseV1 {
    optional uint32 version = 1 [default = 1];
    repeated services.resume.containers.PositionV1 positions = 2;
}
