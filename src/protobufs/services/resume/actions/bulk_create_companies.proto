package services.resume.actions.bulk_create_companies;

import "protobufs/services/resume/containers/resume.proto";

message RequestV1 {
    optional uint32 version = 1 [default = 1];
    repeated services.resume.containers.resume.CompanyV1 companies = 2;
}

message ResponseV1 {
    optional uint32 version = 1 [default = 1];
    repeated services.resume.containers.resume.CompanyV1 companies = 2;
}
