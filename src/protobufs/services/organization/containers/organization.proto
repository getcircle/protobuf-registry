package main.services.organization.containers.organization;

message Organization {
    optional string id = 1;
    optional string name = 2;
    optional string domain = 3;
    optional string image_url = 4;
}
