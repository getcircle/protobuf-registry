package main.credential_service.verify_credentials;

message Request {
    optional string user_id = 1;
    optional string password = 2;
}

message Response {
    optional bool valid = 1 [default = false];
}
