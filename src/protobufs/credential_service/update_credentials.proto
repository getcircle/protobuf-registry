package main.credential_service.update_credentials;

message Request {
    optional string user_id = 1;
    optional string current_password = 2;
    optional string new_password = 3;
}

message Response {}
