package main.identity_service.create_identity;

import "protobufs/identity_service/containers/identity.proto";

message Request {
    optional main.identity_service.containers.identity.Identity identity = 1;
}

message Response {
    optional main.identity_service.containers.identity.Identity identity = 1;
}
